# Global Claude Code Working Rules

@AGENTS.md

Use the imported common working rules, including early delivery for long-running goals, required verification, and the user's checking guide. The English source is normative; `AGENTS.ko.md` is its Korean translation. Do not maintain a separate delivery or testing policy here.

Use Claude Code's own discovery rules for `CLAUDE.md`, applicable `CLAUDE.local.md`, and `.claude/rules/`. The Codex home, `AGENTS.override.md`, and Codex fallback settings in the common document apply only to Codex. Preserve applicable user and project guidance.

Keep this file beside the common `AGENTS.md` when installing, and verify the import in a fresh Claude Code session. Preserve existing custom rules when merging these instructions.
