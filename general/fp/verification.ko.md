# 검증 계약

[영문 원본](verification.md)이 기준이다. [공통 규칙](../AGENTS.md)과 함께 적용하고 정책 변경에는 [개선 절차](evolution.md)도 적용한다.

## 검토 범위 선택

지침 유지보수에서는 Markdown 계약·서술형 사례·링크·번역·출처를 검토한다. CHK-001, CHK-014와 개선 절차를 적용하고 나머지 규칙은 문장 자체의 타당성을 검토한다. 이 문서 검토 완료에 C/Rust 빌드·증명 도구·설치형 검사기·LLM 벤치마크는 필요하지 않다.

실제 적용 소프트웨어를 변경할 때에는 CHK-001부터 CHK-013과 해당 언어 프로파일을 적용한다. 본 패키지의 Markdown 전용 조건이 그 소프트웨어의 필수 테스트를 생략할 권한은 아니다. 패키지 검토는 읽고 확인한 내용만 뒷받침하며 수정하지 않은 외부 프로젝트의 실행 결과를 대신하지 않는다.

**CHK-001** 범위·기준 개정·영향 규칙 ID·입력/관찰 결과·판정 기준·실제로 확보한 근거를 기록한다. 소프트웨어 변경에는 언어/도구 버전과 필수 실행 기준도 기록한다. `PASS(scope, evidence)`, `FAIL(reason)`, `NOT_APPLICABLE(reason)`, `NOT_RUN(reason)`, `APPROVED_EXCEPTION(reference)`를 사용한다. 문서의 PASS는 지정한 본문 검토를 통과했다는 뜻이지 프로그램을 테스트했다는 뜻이 아니다. 예외는 통과가 아니며 미충족 필수 의무는 해당 주장만 차단한다. 적용 작업에 필요한 도구가 없는 것과 문서 작업에 무관한 도구를 구분한다.

**CHK-002** 의미·메모리·종료·자원·동시성 주장을 각각 근거에 연결한다. 신뢰 경계 기록에는 구성요소/버전·승인 계약·가정·검증기와 증명 의무·동적 테스트·빈틈·승인자를 적는다. 증명은 해당 모델의 명제에만 적용된다. 주석·lint·테스트는 서로 대체 가능한 보장 단계가 아니다.

**CHK-003** 변경 동작의 성공·실패·빈 값/부재/0/최소/최대/범위 밖 입력과 도달 가능한 모든 분기를 검사한다. 버그 수정마다 기준 구현에서 실패하고 수정 후 통과하는 재현 사례를 둔다. 오류 코드·mock 호출뿐 아니라 불변식과 실제 상태/효과를 검증한다. 무관한 관심사는 별도 테스트로 둔다.

**CHK-004** 코어는 실제 I/O 없이 검사한다. 독립 예제·속성/모델 검사와 최적화/승인 커널의 단순 순수 참조 모델을 사용한다. 생성기는 유효/무효 입력·상태 전이 이력을 대표하고 입력 유형 커버리지와 폐기 한도를 강제한다. 축소기는 속성의 관련 전제/실패 유형을 보존한다. 무효 입력 테스트에서 유효성 자체를 강제하지 않는다. 무의미하게 통과하는 속성과 구현으로 기대값을 계산하는 테스트를 거부한다.

**CHK-005** 자체 추상화나 대수적 리팩터링 전에 적용 법칙과 전제를 명시한다. 의미가 있을 때 항등·합성·결합·왕복·멱등을 검증하며 부동소수점·부분성·효과 순서에 거짓 법칙을 강제하지 않는다. 주소나 우연한 순회 순서와 독립적으로 정규화·결정적 직렬화를 검사한다.

**CHK-006** 코어 분기 커버리지 100%를 목표로 미도달·실제 도달 불가 사례와 근거를 나열한다. 복합 불리언에는 MC/DC를 적용한다. 변이 테스트로 분기·경계·상태 갱신 결함을 검사하고 동등 변이 등 살아남은 변이를 분류한다. 지표 때문에 방어 코드를 지우거나 단언을 약화하거나 경로를 제외하지 않는다. 도구 미지원은 자동 비해당이 아니라 `NOT_RUN`이다.

**CHK-007** 존재하는 의존성에 첫·N번째·연속·부분 성공 뒤 실패를 주입한다. 타임아웃·취소·할당/저장 공간 부족·부분 획득·정리 실패를 포함한다. 상태 일관성·소유권·실제 효과·남은 작업을 검사한다. 중복/오래된 확인 응답·원격 결과 불명을 확인한다. 로컬 롤백은 원격 롤백 검사가 아니다.

**CHK-008** 동시 시스템은 제어 스케줄과 스트레스·race·데드락·작업 누수·중복 효과·순서를 검사한다. 영속 시스템은 부분 쓰기·강제 종료·재시작·원자성 범위·안전 재시도·보상·복구를 검사한다. 파서/디코더는 panic·크래시·비종료·자원 고갈 퍼징을 수행한다. 입력·시드·실행 순서와 최소 반례를 영구 회귀 사례로 보존한다.

**CHK-009** sleep 기반 단언 대신 시계·난수 시드·스케줄을 제어한다. 퍼징/변이/스트레스에 입력·반복·메모리·시간 예산을 둔다. 유한 실행의 성공은 다른 실행의 부재를 증명하지 않는다. 필수 의무 충족 전에 예산이 끝나면 성공으로 재해석하지 말고 보고한다.

**CHK-010** 언어 프로파일의 타입/효과/의존성 검사와 분석기를 활용한다. 검사기 자체에 허용 예제와 의도적 위반을 넣는다. 부정 테스트는 무관한 컴파일 오류가 아니라 예상 원인으로 실패해야 한다. 간접 호출·중첩 가변 값·필수 문서 누락을 포함한다. 정규식/심벌 검색은 유용한 경보일 수 있지만 건전한 순수성 검사기로 부르지 않는다.

**CHK-011** 빠른 변경/커밋 전 검사와 전체 출시 전 검사를 분리한다. 전체 검증에는 해당하는 지원 주소/메모리/미정의/race/누수 분석을 호환되는 빌드로 실행한다. 지원 OS/아키텍처/엔디언/컴파일러/feature 행렬을 유지하고 비최적화·최적화·배포 결과를 비교한다. 필수 불변식은 배포에서도 유지한다. 미지원 타깃/도구 조합과 게이트 영향을 명시한다.

**CHK-012** 해당하는 실행 시간·할당·최대 생존 메모리·스택 깊이·처리량을 대표 입력으로 측정한다. 입력/도구 조건을 맞추고 필요한 단락 평가·스트림 생산성·오류 순서·정리를 보존한다. 빠른 커널 승인은 속도 수치뿐 아니라 변하지 않은 의미 판정 기준과 FP-007의 근거/승인을 요구한다.

**CHK-013** 출시 후보마다 전체 필수 회귀 검사를 재실행하고 변경된 사용자 흐름·실제 보안 영향·측정 성능·호환성·롤백/복구 단계에 방법/근거/판정이 있는 사람 체크리스트를 만든다. 체크리스트를 언급했다는 이유로 검토가 끝난 것이 아니다. 무관한 위험·승인 절차를 만들지 않는다.

**CHK-014** 본문을 직접 읽는 Markdown 검토로 패키지를 유지보수한다. 아래 목록과 체크리스트를 사용하고 합의된 기준본과 대조하며 정확한 경로·규칙 ID별 발견을 기록한다. 서술형 사례는 계약에 대한 논리적 검토이지 실행한 회귀 테스트가 아니다. 번들 검사기·네이티브 예제·전용 CI 기준은 없다. 구조가 같다는 사실만으로 의미 동등성이나 자동 강제가 성립하지 않는다.

## Markdown 검토 목록

| 기준 문서 | 필수 규칙 ID | 개수 |
| --- | --- | ---: |
| [공통 계약](../AGENTS.ko.md) | GOV-001–006; FP-001–008; FX-001–004; VER-001–003; PKG-001 | 22 |
| [C 프로파일](../profiles/c.ko.md) | C-001–014 | 14 |
| [Rust 프로파일](../profiles/rust.ko.md) | RS-001–014 | 14 |
| [검증](verification.ko.md) | CHK-001–014 | 14 |
| [개선 절차](evolution.ko.md) | EVO-001–010 | 10 |

기준 규칙은 74개이며 각 규칙에는 비어 있지 않은 정의 하나와 한국어 대응 문서의 동일 ID·동등한 의미가 있어야 한다. 양쪽에서 같은 규칙을 지우는 것은 동기화가 아니다. 목록 변경에는 해당 정책 변경과 동일한 근거와 승인이 필요하다. 개수는 추적용 정보이지 품질 점수가 아니다.

## 직접 검토 체크리스트

| 검사 | 필요한 확인 |
| --- | --- |
| 산출물 범위 | 합의된 기준본과 최종 변경 경로를 대조한다. Markdown 지침만 남기고 실행기·워크플로우를 도입하지 않으며, 본문이 삭제한 코드를 다시 생성하도록 시키지 않는지 확인한다. |
| 로딩·링크 | 로컬 링크와 제목 대상을 따라가 설치한 문서 집합 안의 대상·필수 프로파일, 오래된 실행 의존의 부재, 조건부 로딩의 도달 가능성을 확인한다. 관찰 없이 실제 클라이언트 로딩을 주장하지 않는다. |
| 규칙 | 모든 ID·실질적인 본문·적용 조건·예외·상호 참조를 기준본과 대조한다. 중복·누락·빈 규칙·의무 약화는 발견 사항으로 처리한다. UTF-8 텍스트·줄바꿈·마지막 개행·후행 공백·충돌 마커도 확인하되 서식을 의미 검증으로 대체하지 않는다. |
| 번역 | MUST·부정·예외·승인·근거·종료 조건을 중심으로 규칙과 사례의 의미를 대조한다. ID나 줄 수 일치만으로 충분하지 않다. |
| 의미 | 관련 1차 출처를 바탕으로 숨은 효과·별칭·생성 경로·상태/실패 순서·전함수성·정리·합성 법칙을 공격 검토한다. 규범 변경과 해설을 구분한다. |
| 사례 | 서술형 [사례](../evals/cases.ko.md)를 검토하고 기존 ID·의도를 보존하며 새 반례를 추가한다. 기대 답안을 복사하지 말고 규칙·출처에서 판정을 도출한다. |
| 근거 | 문서 검토·실행·가정·미확보 근거를 구분한다. 과거 CI·테스트 결과는 이후 Markdown 전용 개정의 증거가 아니다. |
| 종료 | 수정 후 영향 범위 전체와 양쪽 번역을 다시 검토하고 발견 처리·한계를 기록한다. 깨끗한 재검토는 이번 주기를 끝내는 조건이지 미래 검토의 종료가 아니다. |

## 검토 기록 항목

기준본·후보 식별, 범위, 검토자 구성, 고정한 판정 기준, 읽은 파일·출처, 사례 ID와 논리적 판정, 정확한 문구가 포함된 발견, 수정, 재검토 결과, 미해결 항목, 한계를 기록한다. 자기 파일 안에 자기 커밋 해시를 넣으려 하지 말고 실제 제출 커밋은 생성 후 PR에 기록한다. PASS에는 근거의 범위를 명시한다. 누락·차단을 그대로 기록하며 성공 결과를 미리 채우지 않는다.
